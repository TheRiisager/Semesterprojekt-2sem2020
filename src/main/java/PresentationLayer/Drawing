package PresentationLayer;

import FuntionLayer.LoginSampleException;
import FunktionLayer.Svg;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSerevletResponse;

public class Drawing extends Command {

    @

String execute(HTTPServletRequest, request, HTTPServletResponse, response) throws LoginSampleException)
    Svg svg = new Svg(800,600), "0,0,800,600", 0,0,);
    svg.addRect(0,0,600,780);
    svg.addRect(0,35,4,780);
    svg.addRect(0,565,4,780);
    request.setAttribute("svgdrawing"), svg.toString());
    return "drawing";
}
}